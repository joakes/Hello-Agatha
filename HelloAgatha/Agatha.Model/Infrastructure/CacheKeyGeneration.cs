using System;

namespace Agatha.Model.Infrastructure
{
    public static class CacheKeyGeneration
    {
        public static string GenerateRegionKey(string memberNumber, string region)
        {
            return string.Empty;
        }
        public static string GenerateCacheKey(string memberNumber, Type theRequestType)
        {
            return string.Empty;
        }
        public static string GenerateWebCacheKey(string regionKey, string sessionId, string cacheKey)
        {
            return string.Empty;
        }
    }
}